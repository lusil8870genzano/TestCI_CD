pipeline { 
    agent any
    tools {
        maven 'Maven 3.8.5'
        jdk 'JDK 1.8'
    }  
    stages { 
        stage('Build') { 
            steps { 
               echo 'This is a minimal pipeline.' 
            }
        }
    }
}